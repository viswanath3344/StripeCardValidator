# Uncomment the next line to define a global platform for your project
 platform :ios, '10.0'

target 'StripPaymentGatewayDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
source 'https://github.com/CocoaPods/Specs.git'

  pod 'Stripe'
  pod 'Alamofire', '~> 4.7'

  # Pods for StripPaymentGatewayDemo

  target 'StripPaymentGatewayDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'StripPaymentGatewayDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
